# Dependencias de Python del proyecto.
#   pip install -r requirements.txt
#
# Tkinter NO se instala con pip: viene con el sistema.
#   Debian / Ubuntu:  sudo apt install python3-tk
#
# Tampoco se instalan con pip el cliente de OpenSSH ni ping:
#   sudo apt install openssh-client iputils-ping

customtkinter>=5.2.0
Pillow>=9.0.0

# Solo para la version web opcional (mikrotik_web.py).
# La aplicacion de escritorio no lo necesita.
Flask>=2.0.0
